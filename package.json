{
  "name": "microsoft-oauth-netlify",
  "version": "1.0.0",
  "description": "Microsoft OAuth implementation with refresh tokens using Netlify Functions",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build needed for static site'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@netlify/functions": "^1.4.0",
    "netlify-cli": "^15.0.0"
  }
}
